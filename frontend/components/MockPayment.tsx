"use client";
import{useState}from"react";import{useSearchParams}from"next/navigation";import{API_URL,formatPrice}from"../lib/api";
export default function MockPayment(){const p=useSearchParams();const authority=p.get("authority");const amount=Number(p.get("amount")||0);const[state,setState]=useState("");async function finish(result:"success"|"failed"){const r=await fetch(`${API_URL}/payments/mock/${authority}/`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({result})});const data=await r.json();setState(data.status);setTimeout(()=>location.href="/account/orders",1500)}return <div className="paymentBox"><span className="mockBadge">درگاه آزمایشی</span><h1>پرداخت جانِبی</h1><p>شناسه: {authority}</p><strong>{formatPrice(amount)}</strong>{state?<div className={state==="paid"?"paymentSuccess":"formError"}>{state==="paid"?"پرداخت با موفقیت تایید شد":"پرداخت ناموفق ثبت شد"}</div>:<div className="paymentActions"><button className="button primary" onClick={()=>finish("success")}>پرداخت موفق</button><button className="button ghost" onClick={()=>finish("failed")}>پرداخت ناموفق</button></div>}</div>}
