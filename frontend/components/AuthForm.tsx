"use client";
import{FormEvent,useState}from"react";import{API_URL}from"../lib/api";
export default function AuthForm({mode}:{mode:"login"|"register"}){const[error,setError]=useState("");const[loading,setLoading]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setLoading(true);setError("");const data=Object.fromEntries(new FormData(e.currentTarget));try{const r=await fetch(`${API_URL}/auth/${mode}/`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});const json=await r.json();if(!r.ok)throw new Error(typeof json.errors==="string"?json.errors:"اطلاعات واردشده معتبر نیست.");if(mode==="login"){localStorage.setItem("access_token",json.access);localStorage.setItem("refresh_token",json.refresh);localStorage.setItem("user",JSON.stringify(json.user));location.href=new URLSearchParams(location.search).get("next")||"/account"}else location.href="/login?registered=1"}catch(err){setError(err instanceof Error?err.message:"خطای ناشناخته")}finally{setLoading(false)}}return <form className="authForm" onSubmit={submit}>{mode==="register"&&<div className="twoCols"><label>نام<input name="first_name" required/></label><label>نام خانوادگی<input name="last_name" required/></label></div>}{mode==="register"?<><label>شماره موبایل<input name="phone" inputMode="tel" required placeholder="09121234567"/></label><label>ایمیل<input name="email" type="email" required/></label></>:<label>ایمیل یا شماره موبایل<input name="identifier" required/></label>}<label>رمز عبور<input name="password" type="password" minLength={8} required/></label>{mode==="register"&&<label>تکرار رمز عبور<input name="confirm_password" type="password" minLength={8} required/></label>}{error&&<div className="formError">{error}</div>}<button className="button primary" disabled={loading}>{loading?"لطفاً صبر کنید…":mode==="login"?"ورود به حساب":"ساخت حساب"}</button>{mode==="login"?<p>حساب ندارید؟ <a href="/register">ثبت‌نام کنید</a></p>:<p>قبلاً ثبت‌نام کرده‌اید؟ <a href="/login">وارد شوید</a></p>}</form>}
