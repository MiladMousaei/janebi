import P from "../../components/ContentPage";
import { getStoreConfiguration } from "../../lib/api";
export default async function Page(){const config=await getStoreConfiguration();return <P title="قوانین و شرایط" kicker="شفاف و ساده"><h2>ثبت سفارش</h2><p>قیمت و موجودی در لحظه نهایی‌سازی سفارش در سرور تأیید می‌شود. ثبت اولیه به معنی پرداخت یا رزرو دائمی موجودی نیست.</p><h2>ارسال و پشتیبانی</h2><p>زمان تحویل به مقصد و روش ارسال بستگی دارد. پشتیبانی در بازه «{config.support_hours}» پاسخ‌گو است.</p><h2>بازگشت کالا</h2><p>درخواست بازگشت تا {config.return_days.toLocaleString("fa-IR")} روز پس از تحویل بررسی می‌شود. کالا باید استفاده‌نشده و همراه بسته‌بندی و متعلقات باشد؛ کالای معیوب یا مغایر طبق بررسی پشتیبانی رسیدگی می‌شود. اقلام بهداشتی بازشده قابل بازگشت نیستند.</p></P>}
