import P from "../../components/ContentPage";
import { getStoreConfiguration } from "../../lib/api";
export default async function Page() { const config = await getStoreConfiguration(); return <P title="تماس با ما" kicker="کنار شما هستیم"><section className="contactIntro"><h2>جانِبی؛ همراه مطمئن خرید گجت و لوازم جانبی</h2><p>پیش از خرید، هنگام ثبت سفارش و پس از تحویل کالا می‌توانید از فرم تماس یا تیکت حساب کاربری با پشتیبانی در ارتباط باشید.</p><small>{config.support_hours} پاسخ‌گوی شما هستیم.</small></section><div className="contactCards">{config.support_phone && <a href={`tel:${config.support_phone}`}><b>تلفن پشتیبانی</b><span dir="ltr">{config.support_phone}</span></a>}{config.support_email && <a href={`mailto:${config.support_email}`}><b>ایمیل پشتیبانی</b><span>{config.support_email}</span></a>}<a href="/account/tickets"><b>تیکت پشتیبانی</b><span>ارسال و پیگیری پیام از حساب کاربری</span></a></div>{!config.support_phone && !config.support_email && <p className="contactNotice">شماره و ایمیل پشتیبانی پس از ثبت اطلاعات رسمی فروشگاه در همین صفحه نمایش داده می‌شود.</p>}</P>; }
