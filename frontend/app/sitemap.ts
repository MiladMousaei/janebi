import type{MetadataRoute}from"next";import{safeFetch}from"../lib/api";import type{Paginated,Product}from"../lib/types";export default async function sitemap():Promise<MetadataRoute.Sitemap>{const products=await safeFetch<Paginated<Product>>("/products/?page_size=60",{count:0,next:null,previous:null,results:[]});const base=process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000";return[{url:base,lastModified:new Date(),priority:1},{url:`${base}/shop`,lastModified:new Date(),priority:.9},...products.results.map(p=>({url:`${base}/product/${p.slug}`,lastModified:new Date(),priority:.8}))]}
