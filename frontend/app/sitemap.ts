import type{MetadataRoute}from"next";import{safeFetch}from"../lib/api";import type{Paginated,Product}from"../lib/types";export default async function sitemap():Promise<MetadataRoute.Sitemap>{const products=await safeFetch<Paginated<Product>>("/products/?page_size=60",{count:0,next:null,previous:null,results:[]});const base=process.env.NEXT_PUBLIC_SITE_URL||"http://localhost:3000";const pages=["","/shop","/about","/contact","/faq","/shipping","/returns","/terms","/privacy"];return[...pages.map((path,index)=>({url:`${base}${path}`,lastModified:new Date(),priority:index===0?1:index===1?.9:.65})),...products.results.map(product=>({url:`${base}/product/${product.slug}`,lastModified:new Date(),priority:.8}))]}
